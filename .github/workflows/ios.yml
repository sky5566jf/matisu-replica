name: iOS Theos Build

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Theos
        uses: waruhachi/theos-action@v2.4.6
        with:
          theos-src: 'theos/theos'
          theos-branch: 'master'
          sdks-src: 'theos/sdks'
          sdks-branch: 'master'

      - name: Build Tweak (rootless deb)
        working-directory: ios/tweak
        run: |
          make clean package FINALPACKAGE=1 THEOS_PACKAGE_SCHEME=rootless

      # ============ 引擎模式 1/4：免越狱巨魔引擎（TrollStore tipa） ============
      - name: Build App + package tipa (TrollStore)
        working-directory: ios/app
        run: |
          make clean
          make FINALPACKAGE=1
          BIN=$(find .theos/obj/MatisuAuto.app -name MatisuAuto -type f 2>/dev/null | head -1)
          [ -z "$BIN" ] && { echo "!! binary not built"; exit 1; }
          rm -rf /tmp/tipa
          mkdir -p /tmp/tipa/Payload/MatisuAuto.app
          cp "$BIN" /tmp/tipa/Payload/MatisuAuto.app/MatisuAuto
          cp Info.plist Entitlements.plist /tmp/tipa/Payload/MatisuAuto.app/
          LDID="$THEOS/bin/ldid"; [ -x "$LDID" ] || LDID=$(command -v ldid || true)
          if [ -n "$LDID" ]; then
            "$LDID" -SEntitlements.plist /tmp/tipa/Payload/MatisuAuto.app/MatisuAuto && echo "signed with ldid"
          else
            echo "!! ldid not found; TrollStore will re-sign on install"
          fi
          echo "== tipa app contents =="
          ls -la /tmp/tipa/Payload/MatisuAuto.app
          cd /tmp/tipa && zip -r "$GITHUB_WORKSPACE/matisu-auto.tipa" Payload
          echo "tipa size: $(wc -c < $GITHUB_WORKSPACE/matisu-auto.tipa) bytes"

      # ============ 引擎模式 2/4：有线越狱（rootful deb） ============
      - name: Build App deb (rootful)
        working-directory: ios/app
        run: |
          make clean
          make FINALPACKAGE=1
          cp Info.plist Entitlements.plist .theos/obj/MatisuAuto.app/ 2>/dev/null || true
          LDID="$THEOS/bin/ldid"; [ -x "$LDID" ] || LDID=$(command -v ldid || true)
          [ -n "$LDID" ] && "$LDID" -SEntitlements.plist .theos/obj/MatisuAuto.app/MatisuAuto || true
          make package FINALPACKAGE=1
          mkdir -p ../dist
          mv packages/*.deb ../dist/matisu-auto_rootful.deb
          ls -la ../dist/

      # ============ 引擎模式 3/4：无线越狱 arm64（rootless deb） ============
      - name: Build App deb (rootless arm64)
        working-directory: ios/app
        run: |
          make clean
          make FINALPACKAGE=1 ARCHS=arm64
          cp Info.plist Entitlements.plist .theos/obj/MatisuAuto.app/ 2>/dev/null || true
          LDID="$THEOS/bin/ldid"; [ -x "$LDID" ] || LDID=$(command -v ldid || true)
          [ -n "$LDID" ] && "$LDID" -SEntitlements.plist .theos/obj/MatisuAuto.app/MatisuAuto || true
          make package FINALPACKAGE=1 ARCHS=arm64 THEOS_PACKAGE_SCHEME=rootless
          mkdir -p ../dist
          mv packages/*.deb ../dist/matisu-auto_rootless_arm64.deb
          ls -la ../dist/

      # ============ 引擎模式 4/4：无线越狱 arm64e（rootless deb） ============
      - name: Build App deb (rootless arm64e)
        working-directory: ios/app
        run: |
          make clean
          make FINALPACKAGE=1 ARCHS=arm64e
          cp Info.plist Entitlements.plist .theos/obj/MatisuAuto.app/ 2>/dev/null || true
          LDID="$THEOS/bin/ldid"; [ -x "$LDID" ] || LDID=$(command -v ldid || true)
          [ -n "$LDID" ] && "$LDID" -SEntitlements.plist .theos/obj/MatisuAuto.app/MatisuAuto || true
          make package FINALPACKAGE=1 ARCHS=arm64e THEOS_PACKAGE_SCHEME=rootless
          mkdir -p ../dist
          mv packages/*.deb ../dist/matisu-auto_rootless_arm64e.deb
          ls -la ../dist/

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: matisu-auto-ios
          path: |
            ios/dist/*.deb
            ios/app/packages/*.deb
            ios/tweak/packages/*.deb
            matisu-auto.tipa
          if-no-files-found: warn
